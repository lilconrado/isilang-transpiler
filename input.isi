programa

    declare a, b, c, d, nome, idade, velocidade.
    escreva("Programa Teste").
    escreva("Digite A").
    leia(a).
    escreva("Digite B").
    leia(b).

    se (a < b) entao {
        c := a + b.
    } senao {
        c := a - b.
    }
    nome := "John Doe".
    idade := 20.
    velocidade := 21.31.

    escreva("C e igual a ").
    escreva(c).

    d := c * a + b.

    escreva("D e igual a").
    escreva(d).

fimprog.
