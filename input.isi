programa
    // teste comentario
    inteiro a, b, c, d.
    texto nome.
    inteiro idade.
    real velocidade.
    inteiro gols.
    texto nomeTime.
    texto brasil.

    gols := 1.
    brasil := "Brasil".
    nomeTime := brasil.

    se (gols < 2) {
        escreva("1 < 2").
        escreva("true").
    } senao {
        escreva("1 < 2").
        escreva("false").
    }

    enquanto(gols < 2) {
        gols := 3.
    }
fimprog.
